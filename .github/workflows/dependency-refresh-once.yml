name: BRIX Retired Dependency Refresh

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  retired:
    runs-on: ubuntu-latest
    steps:
      - name: Retired
        run: echo "This one-time dependency migration is retired. Ongoing verification is handled by BRIX Production Gate."
