name: BRIX Retired Dependency Refresh

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  retired:
    runs-on: ubuntu-latest
    steps:
      - name: Retired
        run: echo "This one-time production-hardening workflow is retired. Use the BRIX Production Gate for ongoing verification."
